---
// import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import Link from "@components/Link.astro";
import { SITE, HOME, SOCIALS } from "@consts";

// const blog = (await getCollection("blog"))
//   .filter(post => !post.data.draft)
//   .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
//   .slice(0,SITE.NUM_POSTS_ON_HOMEPAGE);

// const projects = (await getCollection("projects"))
//   .filter(project => !project.data.draft)
//   .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
//   .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <h4 class="animate font-semibold text-black dark:text-white">
      Hi, I'm Yogesh <span class="text-xl">👋🏻</span>
    </h4>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
          <p class="animate">
            I'm a software engineer who loves building things the right way, driven by a product-focused mindset. As a Tech Lead at 314e Corp, I contribute to the Veritable and Practifly products as both a lead and an individual contributor.
          </p>
          <p class="animate">
            At 314e, I have built, grown, and led teams across Search, DevOps, and Data Engineering. With over six years of experience, I specialize in scaling backend services and taking products and teams from 0 to 1 and beyond.
          </p>
        </article>
      </section>

      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">
          Let's Connect
        </h5>
        <article>
          <p>
            If you want to get in touch with me about something or just to say hi,
            reach out on social media or send me an email.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link 
              href="javascript:void(0);" 
              id="email-link" 
              aria-label={`Email ${SITE.NAME}`}
              dataEmail={SITE.EMAIL.split('').reverse().join('')}
            >
              Mail
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>

<script>
  const emailLink = document.getElementById('email-link');
  if (emailLink) {
    emailLink.addEventListener('click', () => {
      const reversedEmail = emailLink.getAttribute('data-email');
      if (reversedEmail) {
        const email = reversedEmail.split('').reverse().join('');
        window.location.href = `mailto:${email}`;
      }
    });
  }
</script>
